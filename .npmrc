# Next.js 16 Turbopack chokes on pnpm's isolated node_modules layout —
# it follows the `next` symlink out of the frontend/ package and decides
# the workspace root is somewhere else. Hoisting flattens that.
# Side effect: scraper deps also become flat, which is fine for Node.
shamefully-hoist=true
